{
  "name": "yafu-mono",
  "private": true,
  "license": "MIT",
  "author": "Ludwig Magnusson <ludwig.magnusson@gmail.com>",
  "scripts": {
    "clean": "pnpm run --parallel clean",
    "lint": "pnpm run --parallel lint",
    "prepublishOnly": "pnpm clean && pnpm test",
    "pretest": "pnpm i && pnpm transpile",
    "test": "pnpm test -r --parallel",
    "transpile": "pnpm run -r transpile"
  },
  "devDependencies": {
    "@yafu/eslint-config-yafu": "^1.0.2",
    "lerna": "^4.0.0",
    "yaml": "^2.0.0"
  }
}
